# @package _global_
task:
  name: train_model
  stage: "03_train_model"
  project_name: "${run.clearml.project_root}/TabularAnalysis/${run.usecase_id}/03_train_model"

train:
  model: ${model_variant.name}
  params: {}
  inputs:
    preprocess_run_dir: null
