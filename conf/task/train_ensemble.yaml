# @package _global_
defaults:
  - /ensemble/base
  - _self_
task:
  name: train_ensemble
  stage: "04_train_ensemble"
  project_name: "${run.clearml.project_root}${run.clearml.project_layout.separator}${run.clearml.project_layout.solution_root}${run.clearml.project_layout.separator}${run.usecase_id}${run.clearml.project_layout.separator}${run.clearml.project_layout.group_map.train_ensemble}"

preprocess:
  variant: ${preprocess_variant.name}

ensemble:
  enabled: true
