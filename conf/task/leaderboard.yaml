# @package _global_
defaults:
  - /leaderboard/scoring
  - _self_
task:
  name: leaderboard
  stage: "05_leaderboard"
  project_name: "${run.clearml.project_root}${run.clearml.project_layout.separator}${run.clearml.project_layout.solution_root}${run.clearml.project_layout.separator}${run.usecase_id}${run.clearml.project_layout.separator}${run.clearml.project_layout.group_map.leaderboard}"

leaderboard:
  train_task_ids: []
  train_run_dirs: []
  require_comparable: true

  top_k: ${exec_policy.limits.max_models}
  dry_run: false
