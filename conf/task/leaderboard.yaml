# @package _global_
task:
  name: leaderboard
  stage: "05_leaderboard"
  project_name: "${run.clearml.project_root}/TabularAnalysis/${run.usecase_id}/05_leaderboard"

leaderboard:
  train_task_ids: []
  train_run_dirs: []
  require_comparable: true

  top_k: ${exec_policy.limits.max_models}
