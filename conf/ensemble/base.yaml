# @package ensemble
enabled: false
method: mean_topk
top_k: 3
selection_metric: ${eval.primary_metric}
exclude_variants: []
fallback_rerun_predict: false

weighted:
  search: random_simplex
  n_samples: 1500
  seed: 42
  top_k_max: 5

stacking:
  meta_model: ridge
  cv_folds: 5
  seed: 42
  require_test_split: false
