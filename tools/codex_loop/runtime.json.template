{
  "cmd": ["bash", "tools/codex_loop/codex_exec.sh"],
  "args": ["{prompt_file}"],
  "timeout_sec": 1800,
  "env": {},
  "notes": "Codex CLI には --prompt-file が無い実装があるため、wrapper(codex_exec.sh)経由で prompt.md を stdin から渡します（codex exec の PROMPT に '-' を指定）。--profile は環境により 'default' が存在しない場合があるため、デフォルトでは指定しません。必要なら args に [\"<profile>\", \"{prompt_file}\"] を指定するか、環境変数 CODEX_PROFILE を使ってください。"
}
